**This file is used to document changes from coding agents**
Logs must be in chronilogical order. Always append new logs to bottom of file.

Example Entry Format:
**12/13/2025** *9:39am CST*: Refactored {file name} from previous set up. Now the file {details of changes}
_____
**12/10/2025** *9:55pm CST*: Added RNG and timer utilities with deterministic tests; introduced util unit test target via CTest; prepared logging/RNG/timer utilities per Phase 2 plan.
**12/13/2025** *10:25am CST*: Implemented card/board utilities and board generator with blocker handling; added deck/board tests and wired `board_tests` into CTest; updated plan Dev Notes for Phase 3.
**12/13/2025** *1:30pm CST*: The project has been moved from a Windows filesystem to WSL (Ubuntu, Linux). The project directory was renamed from: "C++ Poker Solver CFR" to: "poker_solver_cfr"
**12/13/2025** *1:30pm CST*: Added Linux-friendly build guidance and thread linkage; linked test/CLI targets against pthreads; introduced repo `.gitignore` to drop Windows build outputs and normalized test README for WSL use.
**12/13/2025** *1:50pm CST*: Reinstalled cmake and ninja.
**12/13/2025** *2:05pm CST*: Moved pthread discovery to the top-level CMake so Threads::Threads resolves for test targets during Linux builds.
**12/13/2025** *2:20pm CST*: Updated AGENTS.md to mark WSL/Linux as primary environment, prefer Ninja for builds, and point preflop ranges to the current root `preflop-ranges/` directory.
**12/13/2025** *2:45pm CST*: Implemented phase 4 preflop range loader (`io/preflop_range.h`) with normalized combo weights and blocker-aware sampling; added `ranges_tests` covering parse/normalization and blocker exclusion; wired test target into CTest.
**12/13/2025** *3:05pm CST*: Added `scripts/test.sh` helper to configure (preferring Ninja), build, and run all unit tests via a single command.
**12/13/2025** *3:30pm CST*: Implemented phase 5 game state/actions/betting utilities (`core/game_state.h`) with legal action generation, min-raise enforcement, and stack/pot updates; added `actions_tests` for legality, min-raise, short-stack all-in handling, and raise application; all tests passing via CTest.
**12/13/2025** *3:50pm CST*: Hardened `scripts/test.sh` to reuse existing CMake generator (avoiding cache conflicts) and fixed syntax; verified it builds and runs all tests successfully.
**12/13/2025** *4:10pm CST*: Implemented phase 6 tree scaffolding with `core/tree.h` (node representation and deterministic info-set keys) and added `tree_tests` to validate key stability/owner distinction/terminal flag; full test suite passing via CTest.
**12/13/2025** *4:40pm CST*: Implemented phase 7 with a minimal CFR trainer for Kuhn poker (`solver/kuhn_cfr.h`) and added `cfr_tests` to validate convergence sanity (equilibrium structure and response behavior); verified full test suite passing via CTest.
**12/13/2025** *5:20pm CST*: Implemented phase 8 by adding `solver/nlhe_cfr.h` to run CFR over NLHE-style betting actions using the phase-5 legality/apply logic; extended `core/game_state.h` to track per-player committed chips and street completion for correct fold/showdown payoff math; added `nlhe_cfr_tests` covering payoff correctness on a tiny river tree; full test suite passing.
**12/13/2025** *6:05pm CST*: Implemented phase 9 scenario generation via `solver/scenario_generator.h`: samples opener/defender positions, preflop line (open-call vs open-3bet-call), collision-free hole cards from range files, and a blocked random board; builds an initial flop `GameState` with consistent pot/stack/committed fields; added `scenario_tests` and normalized the misnamed `BB_BTN_VS_OPEN (1).json` to `BB_BTN_VS_OPEN.json`.
**12/13/2025** *6:35pm CST*: Implemented phase 10 JSONL output plumbing: added `io/jsonl_writer.h` (append-only writer with single-line enforcement), `io/spot_json.h` (basic schema builder for metadata/ranges/board/metrics with empty strategy placeholders), and `io/minijson.h` used by `jsonl_writer_tests` to validate written JSONL lines parse cleanly and contain required fields.
**12/14/2025** *12:20am CST*: Implemented phase 11 subtree expansion: added `solver/subtree_expansion.h` to prune hero root actions by frequency threshold (>=20%) and solve each selected branch with hero root action locked; extended `solver/nlhe_cfr.h` with root-lock support and villain best-response mode; added `subtree_expansion_tests` validating threshold selection and lock enforcement; full test suite passing.
**12/14/2025** *12:55am CST*: Added deterministic `metadata.spot_id` hashing (`util/hash.h`, `solver/spot_id.h`) and JSONL resume/dedupe scanning (`io/jsonl_dedupe.h`); updated CLI to load existing spot IDs from output and skip duplicates while appending; extended JSON schema builder to include `spot_id`; added `dedupe_tests` validating stable IDs and scan-based dedupe; updated docs in `plan.md` and `AGENTS.md`.
**12/14/2025** *1:15am CST*: Updated `scripts/test.sh` to optionally check an existing JSONL output for duplicate `metadata.spot_id` after tests (defaults to `out.jsonl` if present, or pass a path as arg 2).
**12/14/2025** *1:35am CST*: Implemented phase 12 CLI integration: added `--iterations`, `--branch_threshold`, and progress logging; CLI now runs root CFR + branch solves (villain best-response) per scenario and writes root strategy/branches into JSONL. Added signal handling and per-line flush to ensure completed spots are persisted on early exit.
**12/14/2025** *2:05am CST*: Added `scripts/bench.sh` benchmark runner to build (optional), run N situations, and print throughput (situations/min) plus JSONL-derived stats (avg/p50/p90/p99 solve time, branch counts); documented usage in `tests/READ_ME.md`.
**12/14/2025** *2:20am CST*: Added `upgrade-plan.md` with a phased checklist to reach correctness/benchmark gates required for a meaningful CFR vs CFR+ comparison (without implementing CFR+).
**12/14/2025** *2:40am CST*: Completed upgrade-plan Phase A instrumentation: added per-solve traversal stats to `solver/nlhe_cfr.h` and emitted them in JSONL metrics (`nodes_visited`, `decision_nodes`, `terminal_evals`, `legal_actions_total`, `branch_solves_executed`, `chance_samples`); updated `scripts/bench.sh` to print nodes/sec and avg legal actions/decision; documented benchmark defaults/contract in `tests/READ_ME.md`.
**12/14/2025** *3:05am CST*: Completed upgrade-plan Phase B terminal correctness: added hold’em hand evaluation (`core/hand_rank.h`, `core/hand_evaluator.h`) and integrated correct terminal utility math + all-in runout EV handling (`solver/terminal_utility.h`) into the NLHE CFR path (`solver/nlhe_cfr.h`, `cli/main.cpp`); extended per-spot metrics with runout `chance_samples`; added `hand_evaluator_tests` covering category ranking, wheel straight, fold/showdown chipflow, all-in exact enumeration vs sampling reproducibility, and runout cache behavior.
**12/14/2025** *3:15am CST*: Normalized Linux dev workflow by untracking build artifacts (`build/`, `build_rel/`) and expanding `.gitignore` to ignore Release builds and JSONL outputs.
**12/14/2025** *3:55am CST*: Completed upgrade-plan Phase C betting legality: implemented the `details.md` abstraction in `core/game_state.h` (1/3, 2/3, pot, all-in bet sizes; 2.5x/3.25x/4x/all-in raises; 3-raise cap; simplified short all-in reopen rules) using per-street committed tracking; updated `core/tree.h` info-set keys to include new public betting state; expanded `actions_tests` and adjusted `nlhe_cfr_tests` to cover cap/reopen/short all-in and keep the solver unit tests stable.
**12/14/2025** *4:35am CST*: Implemented upgrade-plan Phase D tree + info-set discipline: added `core/tree_builder.h` to build a deterministic game tree from `GameState` using legality + explicit chance nodes for turn/river deals (with optional sampling); extended `GameState` with public board tracking (`board`, `board_count`) and included board in `core/tree.h` info-set keys; added `tree_builder_tests` and expanded `tree_tests` to validate deterministic keys and board-dependent key separation.
